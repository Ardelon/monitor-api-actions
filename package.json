{
  "name": "monitor-api-actions",
  "version": "1.0.0",
  "description": "Periodically controls services or websites with github actions. Send emails if one of the service is not OK.",
  "main": "checkServices.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.3.1",
    "node-html-parser": "^6.1.10",
    "nodemailer": "^6.9.5"
  }
}
